// /playground — folded into Home as "Try a request" (operator, 2026-08-22).
import { redirect } from 'next/navigation';
export const dynamic = 'force-dynamic';
export default function PlaygroundPage() {
  redirect('/');
}
