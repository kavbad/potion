# extraction-hard-v1

extraction-hard-v1 — dirty documents, not clean ones.
Every document carries at least two of: a distractor identifier, a
superseded value, a field that must be computed, a field that is absent
but conversationally present, an array whose required order differs from
the document listing order, or a boolean stated by negation.
