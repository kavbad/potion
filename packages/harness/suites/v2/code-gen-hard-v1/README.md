# code-gen-hard-v1

code-gen-hard-v1 — retrieval-hostile code generation.
Every item is either a famous problem with one clause changed (so the
memorised answer is wrong) or a composed spec that was never a canonical
exercise. Tests probe the changed clause directly. See manifest.json.
