{
  "description": "A result carrying a THIRD-PARTY key pattern that is not our grant (an OpenRouter-shaped key the redactor cannot know). The Step 3 gate refuses the checkpoint FAIL-CLOSED: a tool that legitimately handles secrets must redact before returning; this one did not, and the run records the typed failure rather than storing the credential.",
  "toolName": "fetch_config",
  "result": "found credentials: sk-or-v1-a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8",
  "expect": "SecretInCheckpointError → run failed 'secret-in-checkpoint'; the tool step is NOT written"
}
