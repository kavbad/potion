# Potion — state of everything, 2026-08-22

One pass across the whole product after the dogfooding round: six agent builds
through the public API, the smoke suite against production, and a read of every
surface a design partner touches. Ranked by what a partner would hit first.

Legend — **Working**: verified live today. **Half-baked**: exists, but a partner
would notice the seam. **Broken / missing**: not there, or wrong.

## Working (verified against production today)

| # | Surface | Evidence |
|---|---|---|
| 1 | Chat completions, OpenAI-compatible | `scripts/smoke-prod.sh` 6/6: 200 + `x-frontier-trace`, `max_tokens` honored, SSE ends with `[DONE]`, cluster hint routes, unknown cluster → 400 `cluster_not_found`, `/v1/models` lists `potion-auto` |
| 2 | Real token streaming | Shipped this deploy: `stream: true` now relays the upstream SSE (OpenAI + OpenRouter transports) instead of replaying a finished answer as a burst. Usage and billed cost come from the final chunk. Measured live after the third deploy: first token at 2.4–3.3 s with the rest spread over ~2 s (before: first token at 4.5–5.7 s, then 64–70 chunks inside 180 ms) |
| 3 | `x-potion-model` on every answer | Every chat/playground response names the model Potion chose; the trace format is unchanged |
| 4 | Sign-in by magic link | Resend verified; links come from `POTION_APP_URL`; sign-out returns to `/home`; kavon@mutiny.ai is an admin |
| 5 | Five-step onboarding | Key → incumbents (+consent) → change one line (+agent instruction blocks) → Try a request (cost / quality / latency, model named) → first receipt |
| 6 | Learning period (the silent audit) | Samples the org's real traffic (PII-redacted, 40/cluster), measures incumbent vs Potion's pick on the org's own usage (≤ $3/org/day), proposes the bar; verified end-to-end on a fresh org |
| 7 | Frontier Notes | `/research`, `/research/2026-w34`, old slugs, `feed.xml`, sitemap, robots, JSON-LD — all 200; written through Potion's own API |
| 8 | Docs assistant | `/api/docs-ask` answers through Potion (`rag-answer` cluster, receipt forwarded), 20/min/IP, 500/day cap |
| 9 | Agent instruction blocks | Per situation (from scratch, from OpenAI SDK, from OpenRouter, AI SDK, Python); three rounds of agent critique folded in |
| 10 | Examples | `examples/node-receipt-check`, `python-triage`, `from-openrouter` run against prod |
| 11 | Fail-closed serving | Budget hard-stop, unknown-model refusal, boot-time frontier↔registry check, tenancy sweep (113 routes classified, enforced by tests) |
| 12 | Test floor | server 694/694, providers 87/87, strategies 67/67 |

## Half-baked (a partner would notice)

| # | Seam | What's wrong | What fixes it |
|---|---|---|---|
| 1 | Classifier separation | Measured on production: centroid cosine 0.39–0.54 in every length bucket, best/runner-up margin 0.025–0.069 (29/41 under 0.05), threshold 0.2 so nothing falls back — the nearest-centroid pick is near a coin flip for most traffic, not only short prompts. Shipped guard: the quality-safe tiebreak (ambiguity never buys a cheaper model; `cluster_tiebreak` on the row) | Better-separated centroids: more exemplars per cluster and a margin-aware training objective — an Observatory research item, measured by the tiebreak rate |
| 2 | ~~Tool calls don't stream~~ | Shipped: the stream transport assembles tool-call fragments by index; text streams live and the calls arrive whole, as the JSON path delivers them. Live probe then showed an empty stream from a reasoning model: the stream body had never sent the tools. Fixed in the same pass |
| 3 | Writer on the OpenAI SDK | Proven, not merged — `openai@7` needs Node 22; the image is node:20 | Bump the image when the next base change lands |

## Broken / missing

| # | Gap | Impact |
|---|---|---|
| 1 | Mixing rungs 4–5 | Not built; the mixing research ladder stops at rung 3. Frontier Notes describe only what is measured |
| 2 | ~~Notion alerts~~ | Shipped: a failed week posts one line to the research log and exits non-zero |
| 3 | ~~Render restore drill~~ | Run 2026-08-23 05:01 UTC: `pg_dump` (428 KB) → scratch `pgvector/pgvector:pg17` → `pg_restore --exit-on-error` → 54 tables, every checked row count identical. Dump kept at `/opt/potion/backups` (0600) |
| 4 | ~~Disk on potion-prod~~ | Reached 83% after three builds; pruned, and a weekly `docker image prune` cron is installed |
| 5 | Tenancy artifact | `artifacts/tenancy-classification.md` is gitignored, so the test's "commit the result" hint is misleading; it regenerates locally and is never shipped |

## Found and fixed in this pass

- `prices.json` had the research test's scan fixtures (`or-mock-nova-1`, `or-mock-apex-1`) committed into the canonical table in `07e6b4d`, and they had rsynced to production. The alias guards kept them unroutable. Restored to the measured roster; `apps/server/test/canonical-prices.test.ts` now pins the committed file.
- The daily-cap test pinned the old 10k value after the deliberate move to 200k.
- `incumbents/roster.ts` and `learning-period.ts` were unclassified in the mock-eligibility inventory.
- The dashboard's AI SDK dependencies had been committed without the lockfile.

## Shipped in this pass (after the audit)

- Real token streaming — through all four provider layers (transport, lazy wrapper, `resilient()`, `withMetrics()`); the first two deploys still burst because each layer rebuilt the provider by hand.
- Quality-safe cluster tiebreak, recorded per request.
- Per-kind-of-work floors (`clusterFloors`), apply-as-merge, apply-all ("Set all N bars").
- Savings baseline = the model the customer designated or named, when it is on the frontier; Usage page "Saved month-to-date" with its basis stated.
- Incumbent roster deduped by native model; a typed model resolves to the roster or the picker says it is not measured yet.
- Observatory failure alert; weekly image prune; restore drill run and passing; runbook corrected (Render, not Neon).
- Found along the way: the production box OOM-killed `dockerd` during a combined image build (≈1 min of downtime, restored on existing images); 2 GB swap added, images now build one at a time.

- OpenAI wire parity (2026-08-23): agentic tool loops (assistant `tool_calls`, `role: "tool"`), content-part arrays, and the caller parameters (`temperature`, `top_p`, `stop`, `seed`, `user`, `response_format`, `parallel_tool_calls`) — found by reading the schema: every tool loop had 400'd on its second turn and `temperature` was accepted then never sent. Images refused with `unsupported_content`.
- Observatory rung 5: the coverage-ratchet report, refreshed weekly (first: roster 29 vs a line at 29.7).
- Classifier: held-out evaluation says 96% at the production threshold; the production margins say the authored held-out set is the wrong instrument — next is a production-derived, judge-labelled set (`docs/research/classifier-separation-2026-08-23.md`).
- Node 22 image.
- Budget-blind frontier (2026-08-23): the extraction pick is a reasoning model that returns nothing under a customer-sized `max_tokens`, and Potion said `finish_reason: stop`. Shipped: finish reason passthrough and the empty-answer retry on the next point (`retry=empty_answer`). Measurement follow-up in `docs/research/budget-blind-frontiers-2026-08-23.md`.

## Order of work from here

1. Production-derived held-out set for the classifier (≈ $1–2), then centroids.
2. Mixing: Observatory rung 6 / MIXING M1 — needs a spend envelope.
3. Frontier Notes writer on the OpenAI SDK (Node 22 is live).
4. MIXING M3 — combinations serving tools and streaming.
5. Measure under customer output budgets (a canary item per reasoning model at `max_tokens: 256`); a `reasoning` flag on the roster.
